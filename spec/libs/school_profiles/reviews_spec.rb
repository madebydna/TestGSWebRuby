require 'spec_helper'

describe SchoolProfiles::Reviews do

  describe "#summary" do
    it "should return summary struct" do
      pending("Build Spec")
      fail
    end
  end

  describe "#reviews_list" do
    it "should return user reviews hash sorted by recent date" do
      pending("Build Spec")
      fail
    end
  end
end
