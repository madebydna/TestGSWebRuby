<%= render partial: 'shared/school_picker', :locals => {
                  display_question:           display_question,
                  preexisting_esp_values:     preexisting_esp_values,
                  response_key:               response_key
          } %>
