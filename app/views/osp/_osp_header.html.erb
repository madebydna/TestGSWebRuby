<div class="pvm bg-light-gray clearfix">
  <div class="limit-width-1200 clearfix prl hidden-xs">
    <button class="btn btn-primary mts fr js-button-link" data-link-value="<%= my_account_url %>">
      dashboard
    </button>
  <h4 class="tal fl gray-header rs-osp_school_name"> <%= @school.name.upcase %></h4>
    <% if current_user.is_esp_demigod? %>
    <%=link_to 'switch schools', osp_dashboard_path, class: 'fl pll mtm pt2' %>
    <% end %>
  </div>

  <div class="visible-xs clearfix prm">
    <button class="btn btn-primary btn-sm mts fr js-button-link" data-link-value="<%= my_account_url %>">
      dashboard
    </button>
    <p class="gray-header default ptm plm rs-osp_school_name"> <%= @school.name.upcase %></p>
    <% if current_user.is_esp_demigod? %>
        <%=link_to 'switch schools', osp_dashboard_path, class: 'plm' %>
    <% end %>
  </div>
</div>