<% unless partners.nil? %>
  <h3 class="tac">Our Partners</h3>
    <div class="js-partners tac">
    <% partners[:partnerLogos].each do |partner| %>
      <%= link_to image_tag(partner[:logoPath], class: 'partner-carousel-image mvm mhl'), 'http://google.com', target: '_blank' %>
    <% end %>
  </div>
<% end %>
