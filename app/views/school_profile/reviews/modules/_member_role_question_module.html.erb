<%
   member_roles = {
           'parent' => t('.parent'),
           'student' => t('.current_student'),
           'teacher' => t('.teacher'),
           'principal' => t('.school_official'),
           'community member' => t('.community_member')
   }
%>

<div class="limit-width-1200 row bg-light-gray gray-box-border js-roleQuestion tac mbl <%= display ? '' : 'dn' %>">
  <div class="col-xs-12 col-md-10 col-md-offset-1 mtl">
    <div class="font-size-large open-sans_sb phs mvl"><%= t('.role_question') %></div>
    <div class="row">
      <%= form_for @school_user, remote: true, url: school_user_path(@school) do |f| %>

          <% member_roles.each do |value, display_value| %>
              <% if value == 'principal' %>
                <div class="clearfix visible-xs hidden-sm hidden-md hidden-lg "></div>
              <% end %>
              <div class="<%= value == 'parent' ? 'col-sm-offset-1 ' : '' %>col-xs-4 col-sm-2 dib mbs">
                <div>
                  <div><%= f.radio_button(:user_type, value, class: 'js-topicalRadioButton') %></div>
                  <div> <%= display_value %> </div>
                </div>
              </div>
          <% end %>
          </div>
          <div>
            <button class="btn btn-primary js-review-question-submit mvl"><%= t('.submit') %></button>
          </div>
      <% end %>
      </div>
</div>
