<%= render 'nav/topnav', hub_params: @hub_params %>

<%= render 'shared/search_hero_prototype', hero_image: @hero_image %>

<%= render 'shared/resources_prototype', kitten_1: @kitten_1,
                                         byline: @byline

                                         %>

<%=render 'shared/offer_prototype'%>

<%=render 'shared/quote_prototype', parent_img: @parent_img%>

<%= render 'shared/who_we_are_prototype', kitten_1: @kitten_1%>

<%=render 'shared/quote_prototype', parent_img: @parent_img%>

<%= render 'shared/join_prototype'%>

<%= render 'shared/state_footer', state: @state, cities: @cities %>