<% content_for :javascript_includes do %>
    <script>
        $(function() {
            console.log('before')
            GS.util.setupImages({ selector: '.is-scaled', desktopImage: '/assets/homepage_hero.jpg', mobileImage: '/assets/hero_mobile.jpg' });
            GS.util.setupImages({ selector: '.is-scaled-hide', desktopImage: '/assets/homepage_picnic.jpg', mobileImage: '' });
        })
    </script>
<% end %>


<%= render 'nav/topnav', hub_params: @hub_params %>

<%= render 'shared/search_hero_prototype', hero_image: @hero_image,
                                           hero_bg_mobile: @hero_bg_mobile %>

<%= render 'shared/resources_prototype', article_1: @article_1,
                                         byline: @byline,
                                         ad_slot_300_250: @ad_slot_300_250
                                         %>

<%=render 'shared/offer_prototype'%>

<%=render 'shared/quote_prototype', parent_img: @parent_img%>

<%= render 'shared/who_we_are_prototype', who_we_are: @who_we_are%>

<%= render 'shared/share_prototype'%>

<%= render 'shared/state_footer', state: @state, cities: @cities %>