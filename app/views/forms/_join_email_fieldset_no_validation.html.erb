<fieldset>
  <%= email_field_tag(
    :email,
    nil,
    {
      id: "join-email",
      placeholder: "#{t('email address', scope: 'forms')}",
      class: 'js-join-email-no-validation',
      required: true
    }
  ) %>
</fieldset>
