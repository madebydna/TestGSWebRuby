<%= render layout:'col', locals: { css_class:'', category_placement: category_placement } do  %>

    <% if action_name == 'overview' %>
        <hr />
        <h3 class="pll">Student Ethnicity</h3>
    <% else %>
        <p style="padding-top: 50px; margin-top: -50px;"></p>
    <% end %>

    <%= render 'shared/pie_chart', data: data, category_placement: category_placement %>

<% end %>
