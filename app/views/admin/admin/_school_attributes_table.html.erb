<% attributes = (['canonical_url'] + school.attributes.keys ).uniq %>
<table class="table table-striped table-bordered">
  <tbody>
    <% attributes.each do |key| %>
    <tr>
      <td><%= key %></td>
      <td><%= school.attributes[key] %></td>
    </tr>
    <% end %>
  </tbody>
</table>