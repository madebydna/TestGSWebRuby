
const span = document.querySelector('.canonical-url-hero');
span.innerText = "<%= root_url.chop + @school.canonical_url %>"

const schoolTable = document.querySelector('.school-attributes-table');
let htmlSnippet = "<%= escape_javascript(render partial: 'school_attributes_table', locals: { school: @school } ) %>"
schoolTable.innerHTML = htmlSnippet;

alert('The canonical url has been regenerate!')