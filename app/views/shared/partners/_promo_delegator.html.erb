<% force_show ||= false %>
<% options ||= {} %>
<% promos.each do |promo| %>
  <% if force_show || promo[:profile_modules].include?(partial) %>
    <%= render partial: "shared/partners/#{promo[:type]}",
              locals: {
                promo: promo,
                options: options
              }
    %>
  <% end %>
<% end %>
