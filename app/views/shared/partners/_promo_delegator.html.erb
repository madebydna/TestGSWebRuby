<% force_show ||= false %>
<% options ||= {} %>
<% promos.each do |promo| %>
  <% if promo[:profile_modules].include?(partial) || force_show %>
    <%= render partial: "shared/partners/#{promo[:type]}",
              locals: {
                promo: promo,
                options: options
              }
    %>
  <% end %>
<% end %>
