import React, { PropTypes } from 'react';

export default class DistrictBoundaries extends React.Component {
  static defaultProps = {
  }

  static propTypes = {
  }

  constructor(props) {
    super(props);
  }

  render() {
    return (<div id="district-boundaries-component"></div>);
  }
}
